fastapi==0.124.0
uvicorn[standard]==0.38.0
python-multipart==0.0.20
pydantic==2.12.5
numpy==2.4.4
scipy==1.17.1
pyloudnorm==0.2.0
imageio-ffmpeg==0.6.0
librosa==0.11.0
noisereduce==3.0.3
sounddevice==0.5.5
# Optional upgrade: pyworld enables WORLD-vocoder formant-preserving pitch
# correction (best quality). Needs MSVC build tools on Windows; the built-in
# spectral-envelope fallback in audio_engine.py is used when absent.
# pyworld
